name: 'App Version'
description: 'Returns App Version'
outputs:
  app-version:
    description: "Application version"
    value: ${{ steps.app-version.outputs.random-id }}
runs:
  using: "composite"
  steps:
    - id: app-version
      run: |
        APP_VERSION=`grep -oP 'version = [^"]*"\K[^"]*' build.gradle*`
        echo "::set-output name=random-id::$(echo $APP_VERSION)"
      shell: bash